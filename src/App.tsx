import { Home } from './Home'

export function App() {
  return <Home />
  /*   return (
    <ThemeProvider theme={defaultTheme}>
      <BrowserRouter>
        <Router />
      </BrowserRouter>
      <GlobalStyle />
    </ThemeProvider>
  ) */
}
